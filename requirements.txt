# =============================================================================
# Pension project Python dependencies
# =============================================================================
# Pinning philosophy:
#   - Application code we directly call:  ==X.Y.Z  (reproducibility wins)
#   - Foundational libs (HTTP, crypto):    >=X.Y,<X+1  (let resolver settle)
#   - Framework versions (dbt):            ==X.Y.Z  (pin core+adapter together)
# Rebuild after edits: docker compose build app --no-cache && docker compose up -d
# =============================================================================

# --- HTTP / utilities ---
requests>=2.32.4,<3.0.0
python-dateutil>=2.9.0,<3.0.0

# --- Data manipulation ---
pandas==2.2.3
pyarrow>=16.1.0,<20.0.0
faker==33.1.0

# --- Config ---
pyyaml>=6.0.2,<7.0.0
python-dotenv>=1.0.1,<2.0.0

# --- Database (Postgres) ---
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# --- Snowflake ---
snowflake-connector-python[pandas]>=4.2.0,<5.0.0

# --- Notebooks ---
jupyterlab>=4.3.4,<5.0.0

# --- dbt (Week 4 Day 6+) ---
# Pin core+adapter to same minor — dbt 1.8+ decoupled core/adapter,
# wide ranges trigger pip backtracking through 14+ versions (Day 6 lesson).
dbt-core==1.11.11
dbt-snowflake==1.11.5